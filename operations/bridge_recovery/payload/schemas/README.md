# Schemas

FastAPI serves the current OpenAPI schema at `/openapi.json`.

This folder is reserved for exported schema artifacts in later construction passes. No static GPT Action schema is claimed in the foundation pass.
