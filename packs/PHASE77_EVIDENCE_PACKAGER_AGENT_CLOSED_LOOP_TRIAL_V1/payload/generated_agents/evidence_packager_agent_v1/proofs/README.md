# Evidence Packager Agent v1 Proofs

Runtime proof files for this generated agent can be placed here when an operator needs agent-local evidence. Factory acceptance proof is owned by Builder under `proofs/`.
